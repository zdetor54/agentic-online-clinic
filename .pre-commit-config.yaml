repos:
  # Run lightweight checks first
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-yaml
      - id: check-json
      - id: check-toml
      - id: pretty-format-json
        args: ['--autofix']
        exclude_types: [jupyter]
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ['--maxkb=5000']
      - id: detect-private-key

  - repo: https://github.com/astral-sh/uv-pre-commit
    # uv version.
    rev: 0.8.11
    hooks:
      - id: uv-lock  # uv.lock always updated
        args: ['--python', '.venv\\Scripts\\python.exe']
      # - id: uv-export   # uncomment if you want uv to generate requirements.txt file


  # Then run the heavier local formatters/linters
  - repo: local
    hooks:
      - id: ruff
        name: "Ruff Linter"
        entry: .venv/Scripts/ruff.exe
        language: system
        args: [check, --fix, --unsafe-fixes]
        types_or: [python, pyi, jupyter]

      - id: ruff-format
        name: "Ruff Formatter"
        entry: .venv/Scripts/ruff.exe
        language: system
        args: [format]
        types_or: [python, pyi, jupyter]

      - id: nbstripout
        name: "Clean Notebooks"
        entry: .venv/Scripts/nbstripout.exe
        language: system
        types: [jupyter]
        args: ["--drop-empty-cells"]

      - id: ty
        name: "ty type checks"
        entry: .venv/Scripts/python.exe
        language: system
        args: [-m, ty, check]
        types_or: [python, pyi, jupyter]
